# 賣貨便結果讀回修正 0.2.3

使用者要求繼續修正已授權的自動轉單流程。匯入成功不等於健康優選已核對成功；完整結果必須通過既有核對與原子回寫。

## 已確認原因及方案

實際頁面原始碼顯示，#downloadResult 按鈕透過 GET /CPF2202/DL?id=結果頁編號 下載。舊助手只讀 href，因而在此停止。直接讀取已確認的官方下載網址，沿用同源、禁止轉址、2 MB上限及Excel檢查。只有精確結果頁路徑與按鈕識別才適配，不執行頁面腳本。

另一方案是攔截全頁下載事件，所需範圍過大且難以唯一對應；手動下載仍保留為恢復方式。這次選擇已確認的特定下載處理。

原批次若已由使用者完成全部核對，恢复時確認成功筆數等於本機原批次筆數、沒有解除保留，才開放多筆。恢復不呼叫上傳。

## 驗證

新增官方按鈕、GET網址、無效路徑、錯誤按鈕、外站連結及人工核對恢復測試，保留完整結果逐欄匹配與禁止重傳測試。正式驗收需重新載入新版擴展。

2026-09-13讀取正式後台時，先前報告的原訂單及批次已確認並已出貨，無須補寫。新提供的結果檔原單號與原批次不一致，已拒絕回填，未修改任何正式訂單。客戶明細及檔案不放入程式碼。
