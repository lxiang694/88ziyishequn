# 0.2.5 工作台狀態同步

使用者截圖顯示0.2.4已連接，首次驗收旗標仍為false，且舊任务保存的賣貨便分頁已失效。完成核對可能發生在別的批次、人工處理或SQL修復中，因此不能只依本機pilotPassed判斷多筆資格。

新增只讀資格端點，有orders.transfer權限才可查詢指定賣場confirmed紀錄是否存在，只回傳boolean。助手從目前健康優選分頁讀取並恢復本機资格。已全部完成的舊attention/paused任務依伺服器批次筆數和狀態同步結束。使用runner同一鎖保護同步，不覆寫執行中任務；查詢失敗不解除限制。

分頁失效時，結果讀取可以重新找唯一的官方結果頁；找不到或多個時提示使用者。只允許讀取，絕不重定向上傳，仍透過伺服器原批次完整比對。

未結束的舊批次不自動丟棄，工作台明示需讀回或保留此批後繼續。保留既有訂單防重與首次無成功紀錄時單筆限制。

驗證包括權限、資格查詢失敗、人工完成同步、執行中輪詢互斥、完整callback訊息鏈與失效分頁只讀尋回。
